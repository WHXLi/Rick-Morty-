<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="transparent_dialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="character_status_circle">
        <item name="android:layout_width">@dimen/character_status_circle_size</item>
        <item name="android:layout_height">@dimen/character_status_circle_size</item>
    </style>

    <style name="character_status_text">
        <item name="android:paddingStart">@dimen/character_status_text_padding</item>
        <item name="android:paddingEnd">@dimen/character_status_text_padding</item>
        <item name="android:textSize">@dimen/character_status_text_size</item>
        <item name="android:textColor">@color/character_main_text</item>
    </style>

    <style name="character_header_text">
        <item name="android:textSize">@dimen/character_header_text_size</item>
        <item name="android:textColor">@color/character_header_text</item>
        <item name="android:gravity">start</item>
        <item name="android:layout_marginStart">@dimen/text_indent</item>
    </style>

    <style name="character_body_text">
        <item name="android:textSize">@dimen/character_body_text_size</item>
        <item name="android:textColor">@color/character_main_text</item>
        <item name="android:gravity">end</item>
        <item name="android:layout_marginEnd">@dimen/text_indent</item>
    </style>

    <style name="circle_button_back">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>